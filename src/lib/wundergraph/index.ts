// WunderGraph Cosmo supergraph + MCP Gateway — Task #2
// Federated supergraph (SBOM, deployment, risk) + per-tool OAuth scopes via @requiresScopes.
// TODO(owner=wundergraph): implement subgraphs and MCP gateway.
export {};
