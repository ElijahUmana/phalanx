// Redis coordination layer — Task #4 (scaffold-data)
// Streams (task distribution) + Pub/Sub (cancellation) + Vector Sets (similarity) + semantic cache.
// Task #4 will add: client.ts, streams.ts, pubsub.ts, vectors.ts, cache.ts, types.ts.
export {};
